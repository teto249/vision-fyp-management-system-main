# AWS MySQL Environment Configuration
# Use these values after creating your RDS MySQL instance

# ===========================================
# 🛢️ AWS RDS MYSQL CONFIGURATION  
# ===========================================

# Database Connection (Update with your RDS endpoint)
NODE_ENV=production
DB_HOST=vision-fyp-mysql.xxxxx.us-east-1.rds.amazonaws.com
DB_NAME=vision_fyp_management_system
DB_USER=admin
DB_PASSWORD=your-rds-master-password
DB_PORT=3306
DB_SSL=true

# Alternative: Connection string format (optional)
# DATABASE_URL=mysql://admin:password@vision-fyp-mysql.xxxxx.us-east-1.rds.amazonaws.com:3306/vision_fyp_management_system

# ===========================================
# 🔐 SECURITY CONFIGURATION
# ===========================================

# Generate secure JWT secret (64+ characters)
JWT_SECRET=your-super-long-random-jwt-secret-key-minimum-64-characters-long
PORT=3000

# ===========================================
# 🌐 FRONTEND & CORS CONFIGURATION
# ===========================================

# Update with your Vercel deployment URL
FRONTEND_URL=https://your-vision-fyp-app.vercel.app

# ===========================================
# 📧 EMAIL CONFIGURATION
# ===========================================

GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=your-16-character-gmail-app-password

# ===========================================
# 📋 AWS RDS SETUP CHECKLIST
# ===========================================

# ✅ 1. Create RDS MySQL instance (db.t3.micro)
# ✅ 2. Choose "Free tier" template
# ✅ 3. Set public access to YES
# ✅ 4. Configure security group (port 3306)
# ✅ 5. Copy RDS endpoint
# ✅ 6. Update DB_HOST with endpoint
# ✅ 7. Set DB_PASSWORD to master password
# ✅ 8. Generate secure JWT_SECRET
# ✅ 9. Test connection locally
# ✅ 10. Deploy to production

# ===========================================
# 💰 FREE TIER LIMITS (12 MONTHS)
# ===========================================

# ✅ 750 hours/month of db.t3.micro
# ✅ 20GB storage
# ✅ 20GB backup storage  
# ✅ 10 million I/O requests

# ===========================================
# 🔗 CONNECTION STRING FORMAT
# ===========================================

# Your connection string will look like:
# mysql://admin:password@vision-fyp-mysql.xxxxx.region.rds.amazonaws.com:3306/vision_fyp_management_system

# Common AWS regions:
# us-east-1 (N. Virginia)
# us-west-2 (Oregon)  
# eu-west-1 (Ireland)
# ap-southeast-1 (Singapore)

# ===========================================
# 📊 MONITORING & ALERTS
# ===========================================

# Set up billing alerts at:
# AWS Billing → Billing preferences → Receive Billing Alerts
# CloudWatch → Alarms → Create alarm for estimated charges

# Monitor free tier usage:
# AWS Billing → Free Tier → Track RDS usage
